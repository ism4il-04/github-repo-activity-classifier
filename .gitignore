# Data files — do NOT commit the full dataset (too large, may contain scraped data)
data/dataset.csv
data/sample.csv
data/raw_cache.json

# Generated plots (outputs of notebooks — reproducible, not versionned)
data/*.png

# Keep the data folder itself
!data/.gitkeep

# Python
__pycache__/
*.py[cod]
*.pyo
.env
venv/
.venv/
*.egg-info/

# Jupyter
.ipynb_checkpoints/

# IDE
.idea/
.vscode/
*.iml

# OS
.DS_Store
Thumbs.db

# --- Ajouts pour la Phase 2 ---
# Ne pas push les gros datasets à la racine
*.csv

# Ne pas push les données transformées
data/processed/

models/best_pipeline_before_tuning.joblib
models/tuned_pipeline.joblib
models/modeling_results.json
models/tuning_metadata.json

# Ignorer le PDF des consignes et les archives
*.pdf
*.zip

# --- Markdown de travail à la racine ---
# Ignorer tous les .md sauf les essentiels
/*.md
!README.md
!DATASET.md
!cadrage.md
!preprocessing_decisions.md

# --- LaTeX build artifacts (docs/) ---
docs/*.aux
docs/*.log
docs/*.lof
docs/*.lot
docs/*.out
docs/*.toc
docs/*.synctex.gz
